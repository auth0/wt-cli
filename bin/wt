#!/usr/bin/env node

var Cli = require('../cli');
var Package = require('../package.json');


var cli = Cli.app({
    description: Package.description,
    version: Package.version,
});


cli.addCategory(require('./profile'));


cli.run();