#!/usr/bin/env node

var Cli = require('../cli');
var Package = require('../package.json');


var cli = Cli.app({
    description: Package.description,
    version: Package.version,
});


cli.addCommand(require('./profile/init'));
cli.addCategory(require('./profile'));
cli.addCommand(require('./logs'));


cli.run();